# Инструкция по подключению к VPS

## 🔐 Подключение через SSH

### Windows

**Вариант 1: PowerShell (встроен в Windows 10/11)**
```powershell
ssh root@45.83.122.17
# или
ssh root@prizmabox.org
```

**Вариант 2: PuTTY (скачать с https://www.putty.org/)**
1. Откройте PuTTY
2. В поле "Host Name" введите: `45.83.122.17` или `prizmabox.org`
3. Port: `22`
4. Connection type: `SSH`
5. Нажмите "Open"
6. При первом подключении подтвердите ключ сервера (Yes)
7. Введите логин: `root`
8. Введите пароль (пароль не будет виден при вводе)

**Вариант 3: Windows Terminal**
```bash
ssh root@45.83.122.17
```

### macOS / Linux

```bash
ssh root@45.83.122.17
# или
ssh root@prizmabox.org
```

---

## 📝 Первое подключение

При первом подключении вы увидите предупреждение:

```
The authenticity of host '45.83.122.17' can't be established.
ECDSA key fingerprint is SHA256:...
Are you sure you want to continue connecting (yes/no)?
```

Введите `yes` и нажмите Enter.

Затем введите пароль (пароль не отображается при вводе).

---

## 🔑 Если забыли пароль

1. Зайдите в панель управления Prohoster
2. Найдите раздел "VPS" → "Управление"
3. Используйте функцию "Сброс пароля" или "VNC консоль"

---

## 🛠️ Альтернативные способы подключения

### Через VNC консоль (если SSH недоступен)

1. Зайдите в панель Prohoster
2. Найдите раздел "VNC консоль" или "Консоль"
3. Откройте консоль в браузере
4. Войдите с логином `root` и паролем

### Через SFTP (для загрузки файлов)

**Windows:**
- Используйте **FileZilla** (https://filezilla-project.org/)
- Host: `sftp://45.83.122.17` или `sftp://prizmabox.org`
- Username: `root`
- Password: ваш пароль
- Port: `22`

**macOS:**
- Используйте **Cyberduck** или встроенный Finder
- В Finder: `Cmd+K` → `sftp://root@45.83.122.17`

**Linux:**
```bash
sftp root@45.83.122.17
```

---

## ✅ Проверка подключения

После подключения выполните:

```bash
# Проверьте систему
uname -a

# Проверьте IP
hostname -I

# Проверьте диск
df -h

# Проверьте память
free -h
```

---

## 🔒 Рекомендации по безопасности

### 1. Создайте пользователя вместо root

```bash
# После подключения как root
adduser appuser
usermod -aG sudo appuser

# Настройте SSH ключи для нового пользователя
su - appuser
mkdir -p ~/.ssh
chmod 700 ~/.ssh
```

### 2. Настройте SSH ключи (опционально)

**На вашем компьютере (Windows):**
```powershell
# В PowerShell
ssh-keygen -t rsa -b 4096
# Нажмите Enter для всех вопросов

# Скопируйте ключ на сервер
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh root@45.83.122.17 "cat >> ~/.ssh/authorized_keys"
```

**На вашем компьютере (macOS/Linux):**
```bash
ssh-keygen -t rsa -b 4096
ssh-copy-id root@45.83.122.17
```

### 3. Отключите вход по паролю (после настройки ключей)

```bash
# На сервере
nano /etc/ssh/sshd_config

# Найдите и измените:
# PasswordAuthentication no
# PermitRootLogin no

# Перезапустите SSH
systemctl restart sshd
```

---

## 📞 Если не можете подключиться

1. **Проверьте пароль** — возможно, он неправильный
2. **Проверьте IP** — убедитесь, что используете правильный IP: `45.83.122.17`
3. **Проверьте файрвол** — убедитесь, что порт 22 открыт
4. **Используйте VNC консоль** в панели Prohoster
5. **Обратитесь в поддержку Prohoster** — они могут помочь с доступом

---

## 🚀 Следующие шаги

После успешного подключения следуйте инструкции из `MIGRATION_VPS.md` для настройки приложения.
